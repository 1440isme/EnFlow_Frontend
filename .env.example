# Proxy dev: trình duyệt gọi /enflow → Next chuyển tiếp tới backend (không cần CORS trên backend)
API_PROXY_TARGET=http://localhost:8080

# Chỉ bật khi gọi thẳng domain API (cần CORS). Để trống = dùng proxy /enflow
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
